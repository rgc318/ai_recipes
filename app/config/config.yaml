server:
  host: "0.0.0.0"
  port: 8000
  log_level: "info"

database:
  url: "postgresql+asyncpg://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

minio:
  endpoint: "192.168.31.229:19000"
  access_key: "${MINIO_ACCESS_KEY}"
  secret_key: "${MINIO_SECRET}"
  bucket_name: "ai-recipes"
  secure: false

logging:
  enable_file: true
  log_dir: "logs"
  rotation: "1 week"
  retention: "1 month"
